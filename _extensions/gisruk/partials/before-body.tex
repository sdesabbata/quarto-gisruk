% Modified from the original GISRUK LaTeX template and
% the quarto-journals/elsevier template (MIT license)
% https://github.com/quarto-journals/elsevier/blob/main/_extensions/elsevier/partials/before-body.tex


\pagenumbering{gobble} % Turn off page numbers

\sectionfont{\normalsize}
\subsectionfont{\normalsize}
\subsubsectionfont{\normalsize}
\paragraphfont{\normalsize}

\renewcommand{\abstractname}{Summary} % Make 'abstract' be called 'Summary'

% **************  TITLE AND AUTHOR INFORMATION **************

\title{$title$}

$for(by-author)$\author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\corref{cor1}$endif$%
$if(by-author.note.text)$\fnref{fn$by-author.note.number$}$endif$}
$if(by-author.email)$ \thanks{$by-author.email$} $endif$
$endfor$
$for(by-affiliation)$
\affil[$it.number$]{organization={$it.name$$if(it.department)$, $it.department$$endif$},$if(it.address)$addressline={$it.address$}$endif$$if(it.city)$$if(it.address)$,$endif$city={$it.city$}$endif$$if(it.country)$$if(it.city)$,$elseif(it.address)$,$endif$country={$it.country$},countrysep={,}$endif$$if(it.postal-code)$$if(it.country)$,$elseif(it.city)$,$elseif(it.address)$,$endif$postcode={$it.postal-code$}$endif$,postcodesep={}}
$endfor$

\renewcommand\Authands{ and } % correct last comma in author list

% **************  ABSTRACT/SUMMARY  **************

\begin{abstract}
\centering

$abstract$

{\bf KEYWORDS:} $for(keywords/allbutlast)$$keywords$ \unskip, $endfor$ $for(keywords/last)$$keywords$$endfor$

\end{abstract}
